# 200% run — continuation state

**Resume rule:** read this file, find the first unchecked item, continue from there.
Do not redo checked work. Commit at each checkpoint. Verify in the browser, not just `tsc`.

Baseline this run started from: `6d4b9b2` (working, 60fps locked, all suites pass).

## Environment

- Dev server: `npx vite --host 127.0.0.1 --port 5173 --strictPort` (needs sandbox bypass).
- Verify: `scripts/verify-room.mjs`, `verify-shelf.mjs`, `verify-shelf-a11y.mjs`,
  `verify-a11y.mjs`, `verify-motion.mjs`, `startup-trace.mjs`, `profile.mjs`, `gallery.mjs`.
- All Playwright scripts need `dangerouslyDisableSandbox` (Chrome profile dir).

## External 3D assets — blocked, decided

Sketchfab `/v3/models/{uid}/download` returns 401; Poly Pizza `/v1.1` returns 401. Both need
account credentials this session was not given. Search/metadata works, downloads do not.
**Decision:** research real reference characteristics over the web, hand-model to a much higher
standard. If the user supplies a Sketchfab token later, the GLTF path can be added.

## Checkpoints

### A — hero bugs + hero models
- [x] A1 bright light = point light reflecting as a pinpoint specular in near-mirror window
      glass. Glass roughness 0.04 -> 0.16, accent point light -> RectAreaLight.
- [x] A2 sill top face was coplanar with the wall piece top -> z-fight band across the window.
      Wall now stops 20mm short, sill interpenetrates it, albedo 0.92 -> 0.72.
- [x] A3 MacBook rebuilt on an extruded riser profile; tilt sign was inverted and the extrude
      was mirrored in z (tall back post landed at the front).
- [x] A4 speedcube rebuilt stickerless: colour edge-to-edge on each piece, frosted finish,
      centre cap rings, wide seams. Researched: stickerless + frosted + large radii is what
      reads as a GAN. No logo (trademarked).
- [x] A5 mouse is now a parametric dome over a tapered footprint (an extruded outline gives a
      flat-topped puck). Seams generated from the same surface so they cannot float.
- [x] A6 keycaps sculpted (taper + dish), per-row tilt, stabiliser bars, sub-mm seat jitter.
- [ ] A7 monitor/lamp/electronics/bins/books detail pass (§5)

### B — cinematic lighting + atmosphere  <-- NEXT

Note from A4: at night, faces turned away from lamp/window go almost black (the cube's front
face). The fill tier is too low. Fix in B3.

- [ ] B1 lamp volumetric rays (§11)
- [ ] B2 sun shafts through the window (§12)
- [ ] B3 light hierarchy: key / practical / secondary / accent / fill (§10)
- [ ] B4 filmic grade: curve, bloom, vignette, grain, exposure (§13)
- [ ] B5 background fade + depth treatment (§30)

### C — interaction
- [ ] C1 bookshelf: one book per committed gesture, MANDATORY (§19–22)
- [ ] C2 cursor guidance / proximity awareness (§23)
- [ ] C3 project hover response (§24)
- [ ] C4 camera zoom easing + framing (§25)
- [ ] C5 panel typography/material (§26)

### D — music widget with a real source (§27)
- [ ] D1 legitimate embed/preview, replaceable source, in-scene placement

### E — performance + final verification
- [ ] E1 profile every scenario in §39
- [ ] E2 fix highest-value bottlenecks
- [ ] E3 full suite + gallery + startup trace

## Notes for the resuming session

(append findings here as work proceeds)
